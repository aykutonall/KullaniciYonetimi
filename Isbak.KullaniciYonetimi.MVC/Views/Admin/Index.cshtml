
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@if(User.IsInRole("Admin"))
{
    <p>Sen Admin'sin.</p>
}else if(User.IsInRole("SystemAdmin"))
{
    <p>Sen SystemAdmin'sin.</p>
}else
{
    <p>Sen sıradan bir User'sın.</p>
}
